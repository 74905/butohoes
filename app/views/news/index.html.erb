  <main>
  <div class="container">
    <div class="row">
        <div class="col-lg-2">
        <span class="index-title">NEWS</span>
        </div>
        <div class="col-lg-10">
            <div class="row">
                <% @news.each do |news| %>
          <div class="col-lg-4 col-xs-12 mb-3">
          <%= link_to news_path(news.id), data: {"turbolinks" => false} do %>
            <div class="card bg-light h-100" style="width: 15rem; margin: 0 auto;">
                  <%= attachment_image_tag news, :news_image,　format: 'png', fallback: "news.jpg", size: "237x200" %>
                <div class="card-body">
                  <p class="card-text"><%= news.title%></p>
                  <p class="card-text"><small class="text-muted"><%= news.created_at.strftime('%Y-%m-%d') %></small></p>
                </div>
            </div>
          <% end %>
          </div>
        <% end %>
            </div>
        </div>
    </div>
  </div>
  </main>
<%= javascript_include_tag 'aimsition.js' %>
<%= javascript_include_tag 'news.js' %>